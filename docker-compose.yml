version: '3.8'

services:
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ML_MODE=rinalmo_stub
    volumes:
      - ./backend:/app
    restart: unless-stopped
